import { MeetupInCityPipe } from './meetup-in-city.pipe';

describe('MeetupInCityPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetupInCityPipe();
    expect(pipe).toBeTruthy();
  });
});
