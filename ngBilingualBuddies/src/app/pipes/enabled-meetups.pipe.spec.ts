import { EnabledMeetupsPipe } from './enabled-meetups.pipe';

describe('EnabledMeetupsPipe', () => {
  it('create an instance', () => {
    const pipe = new EnabledMeetupsPipe();
    expect(pipe).toBeTruthy();
  });
});
