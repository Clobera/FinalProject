import { MeetupSearchPipe } from './meetup-search.pipe';

describe('MeetupSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetupSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
