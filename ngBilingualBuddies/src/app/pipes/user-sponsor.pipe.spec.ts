import { UserSponsorPipe } from './user-sponsor.pipe';

describe('UserSponsorPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSponsorPipe();
    expect(pipe).toBeTruthy();
  });
});
