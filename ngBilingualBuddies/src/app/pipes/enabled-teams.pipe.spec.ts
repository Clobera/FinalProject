import { EnabledTeamsPipe } from './enabled-teams.pipe';

describe('EnabledTeamsPipe', () => {
  it('create an instance', () => {
    const pipe = new EnabledTeamsPipe();
    expect(pipe).toBeTruthy();
  });
});
