import { UserLanguagePipe } from './user-language.pipe';

describe('UserLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new UserLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
